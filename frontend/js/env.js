/**
 * ReelVault — API Base URL Configuration
 *
 * Before deploying to Vercel, update REELVAULT_API_URL to your Railway backend URL.
 * Example: window.REELVAULT_API_URL = 'https://reelvault-production.up.railway.app';
 *
 * Leave empty string to use relative URLs (when frontend is served from Spring Boot).
 */
window.REELVAULT_API_URL = '';
